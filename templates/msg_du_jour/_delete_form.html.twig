<form method="post" action="{{ path('msg_du_jour_delete', {'id': msg_du_jour.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce message ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ msg_du_jour.id) }}">
    <button class="btn">Delete</button>
</form>
