<form method="post" action="{{ path('image_slide_delete', {'id': image_slide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_slide.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
