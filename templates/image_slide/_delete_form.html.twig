<form method="post" class="delete-form" action="{{ path('image_slide_delete', {'id': image_slide.id}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_slide.id) }}">
    <button class="btn btn-danger" type="submit" onclick="deleteSlide()" >supprimer</button>
</form>
