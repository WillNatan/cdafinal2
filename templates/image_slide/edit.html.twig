{% extends 'admin.html.twig' %}

{% block title %}Edit ImageSlide{% endblock %}

{% block body %}
    <h1>Edition</h1>

    {{ include('image_slide/_form.html.twig', {'button_label': 'Modifier'}) }}{{ include('image_slide/_delete_form.html.twig') }}

    <a href="{{ path('image_slide_index') }}" class="btn-primary btn">Retour à la liste</a>


{% endblock %}
