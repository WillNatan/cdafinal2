{% extends 'admin.html.twig' %}

{% block title %}ImageSlide{% endblock %}

{% block body %}

    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-icon card-header-rose">
                <div class="card-icon">
                    <i class="material-icons">assignment</i>
                </div>
                <h4 class="card-title ">{{ image_slide.alternative }}</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <tbody>
                        <tr>
                            <th>Identifiant</th>
                            <td>{{ image_slide.id }}</td>
                        </tr>
                        <tr>
                            <th>Nom de l'image</th>
                            <td>{{ image_slide.imageUrl }}</td>
                        </tr>
                        <tr>
                            <th>Alternative</th>
                            <td>{{ image_slide.alternative }}</td>
                        </tr>
                        <tr>
                            <th>Image</th>
                            <td><img src="{{ asset('slides/'~image_slide.imageUrl) }}" alt="{{ image_slide.alternative }}" class="img-fluid" width="100" height="100"></td>
                        </tr>
                        </tbody>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <table class="table">

    </table>

        <div class="row">
            <div class="col-md-2">
                <div class="d-flex justify-content-center flex-column">
                    <a href="{{ path('image_slide_index') }}" class="btn btn-primary">Retour à la liste</a>
                    <a href="{{ path('image_slide_edit', {'alternative': image_slide.alternative}) }}" class="btn btn-warning"><i class="fa fa-edit"></i></a>
                </div>
            </div>
        </div>


    {{ include('image_slide/_delete_form.html.twig') }}
{% endblock %}
