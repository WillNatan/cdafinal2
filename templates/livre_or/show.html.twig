{% extends 'admin.html.twig' %}

{% block title %}LivreOr{% endblock %}

{% block body %}
    <h1>LivreOr</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre_or.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ livre_or.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ livre_or.date ? livre_or.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ livre_or.statut ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ livre_or.Username }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livre_or_index') }}">back to list</a>

    <a href="{{ path('livre_or_edit', {'id': livre_or.id}) }}">edit</a>

    {{ include('livre_or/_delete_form.html.twig') }}
{% endblock %}
